import { adminConfig, adminPageMeta } from "./admin";
import { issuerConfig, issuerPageMeta } from "./issuer";
import { userConfig, userPageMeta } from "./user";

export const SidebarNavigation = {
  admin: adminConfig,
  issuer: issuerConfig,
  user: userConfig,
};

export { adminPageMeta, issuerPageMeta, userPageMeta };
